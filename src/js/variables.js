export let posYmouse = innerHeight / 2; // posición del mouse en el eje Y - para la máscara y linea de lectura

export const setPosYMouse = (value) =>{
    posYmouse = value
}

export let touchDevice = false; // indica si el dispositivo es táctil

export const setTouchdevice = (value) => {
    touchDevice = value
}